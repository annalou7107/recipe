<p id="notice"><%= notice %></p>

<h1>Recipe Items</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Image</th>
      <th>Ingredients</th>
      <th>Directions</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @recipe_items.each do |recipe_item| %>
      <tr>
        <td><%= recipe_item.title %></td>
        <td><%= recipe_item.image %></td>
        <td><%= recipe_item.ingredients %></td>
        <td><%= recipe_item.directions %></td>
        <td><%= link_to 'Show', recipe_item %></td>
        <td><%= link_to 'Edit', edit_recipe_item_path(recipe_item) %></td>
        <td><%= link_to 'Destroy', recipe_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Recipe Item', new_recipe_item_path %>
