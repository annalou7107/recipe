<%= form_with(model: recipe_item, local: true) do |form| %>
  <% if recipe_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipe_item.errors.count, "error") %> prohibited this recipe_item from being saved:</h2>

      <ul>
      <% recipe_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :recipe_item_title %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :recipe_item_image %>
  </div>

  <div class="field">
    <%= form.label :ingredients %>
    <%= form.text_field :ingredients, id: :recipe_item_ingredients %>
  </div>

  <div class="field">
    <%= form.label :directions %>
    <%= form.text_area :directions, id: :recipe_item_directions %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
